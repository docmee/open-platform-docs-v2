---
title: '修改大纲（V1）'
---

# 修改大纲（V1）

## 接口信息

- 方法：`POST`
- 路径：`/ppt/updateOutline`
- 鉴权：请求头传 `token`

## 接口作用

`V1` 兼容接口。根据用户指令重新组织现有大纲结构。

## 请求参数

```json
{
  "outlineMarkdown": "# 主题\n## 章节\n### 页面标题\n#### 内容标题",
  "length": "medium",
  "question": "让大纲更专业"
}
```

## 使用说明

- `question` 可传系统内置优化指令，也可传用户自定义要求
- 返回为流式结构，响应格式与 [`generateOutline`](/api-reference/v1/generate-outline) 一致
