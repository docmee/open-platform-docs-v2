---
title: '获取 PPT 列表'
---

# 获取 PPT 列表

## 接口信息

- 方法：`POST`
- 路径：`/api/ppt/listPptx`
- 鉴权：请求头传 `token`

## 接口作用

分页查询当前用户下的 PPT 作品列表。

## 请求参数

```json
{
  "page": 1,
  "size": 10
}
```

## 返回内容

每条记录通常包含 `id`、`subject`、`coverUrl`、`templateId`、`createTime` 等字段。
