---
title: '分页查询模板'
---

# 分页查询模板

## 接口信息

- 方法：`POST`
- 路径：`/api/ppt/templates`
- 鉴权：请求头传 `token`

## 接口作用

分页查询系统模板或用户模板列表。

## 请求参数

```json
{
  "page": 1,
  "size": 10,
  "filters": {
    "type": 1,
    "category": null,
    "style": null,
    "themeColor": null
  }
}
```

## 关键字段

- `filters.type`：必传，`1` 为系统模板，`4` 为用户模板
- `coverUrl`：模板封面地址，访问时需要拼接 `?token=xxx`

## 使用说明

- 支持在 URL 上通过 `lang` 指定国际化语言
- 可与 [`template/options`](/api-reference/templates/template-options) 配合使用

## 多语言示例

<ApiCodeTabs
  node={`const response = await fetch('https://docmee.cn/api/ppt/templates', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    token: 'YOUR_TEMP_TOKEN'
  },
  body: JSON.stringify({
    page: 1,
    size: 10,
    filters: {
      type: 1,
      category: null,
      style: null,
      themeColor: null
    }
  })
})

console.log(await response.json())`}
  python={`import requests

response = requests.post(
    'https://docmee.cn/api/ppt/templates',
    headers={
        'Content-Type': 'application/json',
        'token': 'YOUR_TEMP_TOKEN'
    },
    json={
        'page': 1,
        'size': 10,
        'filters': {
            'type': 1,
            'category': None,
            'style': None,
            'themeColor': None
        }
    }
)

print(response.json())`}
  go={`package main

import (
  "bytes"
  "fmt"
  "io"
  "net/http"
)

func main() {
  body := []byte(\`{"page":1,"size":10,"filters":{"type":1,"category":null,"style":null,"themeColor":null}}\`)
  req, _ := http.NewRequest("POST", "https://docmee.cn/api/ppt/templates", bytes.NewBuffer(body))
  req.Header.Set("Content-Type", "application/json")
  req.Header.Set("token", "YOUR_TEMP_TOKEN")

  resp, _ := http.DefaultClient.Do(req)
  defer resp.Body.Close()
  data, _ := io.ReadAll(resp.Body)
  fmt.Println(string(data))
}`}
  java={`import java.net.URI;
import java.net.http.HttpClient;
import java.net.http.HttpRequest;
import java.net.http.HttpResponse;

public class ListTemplatesExample {
  public static void main(String[] args) throws Exception {
    String body = """
      {
        "page": 1,
        "size": 10,
        "filters": {
          "type": 1,
          "category": null,
          "style": null,
          "themeColor": null
        }
      }
      """;

    HttpRequest request = HttpRequest.newBuilder()
      .uri(URI.create("https://docmee.cn/api/ppt/templates"))
      .header("Content-Type", "application/json")
      .header("token", "YOUR_TEMP_TOKEN")
      .POST(HttpRequest.BodyPublishers.ofString(body))
      .build();

    HttpResponse<String> response = HttpClient.newHttpClient()
      .send(request, HttpResponse.BodyHandlers.ofString());

    System.out.println(response.body());
  }
}`}
/>
