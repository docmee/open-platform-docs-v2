---
title: '获取最新任务数据'
---

# 获取最新任务数据

## 接口信息

- 方法：<HttpMethodBadge method="GET" />
- 路径：<InlineCode value="https://open.docmee.cn/api/ppt/v2/latestData">https://open.docmee.cn/api/ppt/v2/latestData</InlineCode>
- 鉴权：请求头传 `token`

## 接口作用

在 AIL 智能布局任务执行过程中，拉取当前任务最新的分页 `HTML` 和任务进度，适合在 SSE 中断、超时或前端需要恢复现场时使用。

## 请求示例

```bash
curl --request GET \
  --url "https://open.docmee.cn/api/ppt/v2/latestData?taskId=task_xxx&from=0" \
  --header "token: YOUR_TEMP_TOKEN"
```

## 请求参数

| 参数名 | 类型 | 必填 | 示例 | 说明 |
| --- | --- | --- | --- | --- |
| `taskId` | `string` | 是 | `task_xxx` | 任务 ID。 |
| `from` | `number` | 否 | `0` | 起始页索引。传入后可只拉取指定页及之后的最新内容。 |

## 响应示例

```json
{
  "code": 0,
  "message": "操作成功",
  "data": {
    "status": "running",
    "step": 2,
    "payload": {
      "total": 8,
      "current": 5,
      "htmlMap": {
        "0": "<section class=\"page cover\">...</section>",
        "1": "<section class=\"page content\">...</section>"
      }
    }
  }
}
```

## 响应字段说明

| 字段名 | 类型 | 示例 | 说明 |
| --- | --- | --- | --- |
| `code` | `number` | `0` | 通用响应状态码，`0` 表示成功。 |
| `message` | `string` | `操作成功` | 服务端返回的结果说明。 |
| `data.status` | `string` | `running` | 当前任务状态，常见值为 `running`、`completed`。 |
| `data.step` | `number` | `2` | 当前执行步骤。 |
| `data.payload.total` | `number` | `8` | 总页数。 |
| `data.payload.current` | `number` | `5` | 当前已推进到的页序号或页数。 |
| `data.payload.htmlMap` | `object` | `{"0":"<section ...>"}` | 已生成页面的 `HTML` 映射，键为页索引。 |

## 使用说明

- `latestData` 是 AIL 执行阶段的恢复接口，不替代 [`generatePptxByAi`](/api-reference/v2/generate-pptx-by-ai)。
- 如果 `htmlMap` 已经包含全部页面，仍建议继续调用 [`getConvertResult`](/api-reference/v2/get-convert-result) 确认最终转换是否完成。
- 适合在前端断线重连、刷新页面、超时重试等场景下使用。

## 多语言示例

<ApiCodeTabs items={['cURL', 'Node.js', 'Python', 'Go', 'Java']}>
  <ApiCodeTab>

```bash
curl --request GET \
  --url "https://open.docmee.cn/api/ppt/v2/latestData?taskId=task_xxx&from=0" \
  --header "token: YOUR_TEMP_TOKEN"
```

  </ApiCodeTab>
  <ApiCodeTab>

```js
const response = await fetch(
  'https://open.docmee.cn/api/ppt/v2/latestData?taskId=TASK_ID&from=0',
  {
    headers: {
      token: 'YOUR_TEMP_TOKEN'
    }
  }
)

console.log(await response.json())
```

  </ApiCodeTab>
  <ApiCodeTab>

```python
import requests

response = requests.get(
    'https://open.docmee.cn/api/ppt/v2/latestData',
    headers={'token': 'YOUR_TEMP_TOKEN'},
    params={'taskId': 'TASK_ID', 'from': 0}
)

print(response.json())
```

  </ApiCodeTab>
  <ApiCodeTab>

```go
package main

import (
  "fmt"
  "io"
  "net/http"
)

func main() {
  req, _ := http.NewRequest("GET", "https://open.docmee.cn/api/ppt/v2/latestData?taskId=TASK_ID&from=0", nil)
  req.Header.Set("token", "YOUR_TEMP_TOKEN")

  resp, _ := http.DefaultClient.Do(req)
  defer resp.Body.Close()
  data, _ := io.ReadAll(resp.Body)
  fmt.Println(string(data))
}
```

  </ApiCodeTab>
  <ApiCodeTab>

```java
import java.net.URI;
import java.net.http.HttpClient;
import java.net.http.HttpRequest;
import java.net.http.HttpResponse;

public class LatestDataExample {
  public static void main(String[] args) throws Exception {
    HttpRequest request = HttpRequest.newBuilder()
      .uri(URI.create("https://open.docmee.cn/api/ppt/v2/latestData?taskId=TASK_ID&from=0"))
      .header("token", "YOUR_TEMP_TOKEN")
      .GET()
      .build();

    HttpResponse<String> response = HttpClient.newHttpClient()
      .send(request, HttpResponse.BodyHandlers.ofString());

    System.out.println(response.body());
  }
}
```

  </ApiCodeTab>
</ApiCodeTabs>
